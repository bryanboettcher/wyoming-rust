# Cross-compilation is handled by the Dockerfile via build ARGs.
# No local cross-compilation configuration needed here.
#
# For Docker builds, see the examples in the Dockerfile header.
# For local cross builds using `cross` tool:
#   cargo install cross --git https://github.com/cross-rs/cross
#   cross build --release --target arm-unknown-linux-gnueabihf
