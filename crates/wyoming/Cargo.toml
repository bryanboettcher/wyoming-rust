[package]
name = "wyoming"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Wyoming protocol library for Home Assistant voice pipelines"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
thiserror = "2"

[dev-dependencies]
