[satellite]
name = "test-satellite"
area = "Test Room"

[server]
mode = "listen"
host = "0.0.0.0"
port = 10700

[audio]
wav_input = "/data/test.wav"
rate = 16000
width = 2
channels = 1
chunk_ms = 20

[vad]
mode = "always_on"
silence_timeout_ms = 3000

# No [[feedback]] configured — defaults to logging-only provider.
